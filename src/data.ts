interface Data {
  switchState: boolean;
  firstName: string;
  radioValue: boolean;
  checkedTools: string[];
}

const data: Data = {
  switchState: true,
  firstName: "",
  radioValue: false,
  checkedTools: [],
};

export default data;
